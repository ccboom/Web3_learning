# Git 版本管理与回滚指南

## 📌 当前版本

- **最新版本**: v1.0.0
- **发布日期**: 2026-01-23
- **内容**: Week 1-7 完整学习内容

---

## 🏷️ 版本标签管理

### 创建版本标签

```bash
# 为当前提交创建带注释的标签
git tag -a v1.1.0 -m "版本描述信息"

# 为指定提交创建标签
git tag -a v1.0.1 <commit-hash> -m "版本描述"

# 查看所有标签
git tag

# 查看标签详情
git show v1.0.0
```

### 推送标签到远程

```bash
# 推送单个标签
git push origin v1.0.0

# 推送所有标签
git push origin --tags

# 删除本地标签
git tag -d v1.0.0

# 删除远程标签
git push origin --delete v1.0.0
```

---

## 🔄 版本回滚方法

### 方法 1: 使用标签回滚 (推荐)

```bash
# 1. 查看所有版本标签
git tag

# 2. 回滚到指定版本
git reset --hard v1.0.0

# 3. 强制推送到远程 (覆盖错误的提交)
git push -f origin main
```

### 方法 2: 使用提交 SHA 回滚

```bash
# 1. 查看提交历史
git log --oneline

# 2. 回滚到指定提交
git reset --hard <commit-sha>

# 3. 强制推送
git push -f origin main
```

### 方法 3: 创建回滚提交 (保留历史)

```bash
# 创建一个新提交来撤销之前的更改
git revert <commit-sha>

# 推送到远程
git push origin main
```

---

## 📋 语义化版本号规范

采用 `v主版本号.次版本号.修订号` 格式:

- **主版本号 (Major)**: 重大变更,不兼容的 API 修改
  - 例: v1.0.0 → v2.0.0
  
- **次版本号 (Minor)**: 新增功能,向下兼容
  - 例: v1.0.0 → v1.1.0
  
- **修订号 (Patch)**: Bug 修复,向下兼容
  - 例: v1.0.0 → v1.0.1

### 示例版本演进

```
v1.0.0 - 初始版本 (Week 1-7)
v1.1.0 - 添加 Week 8 内容
v1.1.1 - 修复 Week 8 中的错误
v1.2.0 - 添加 Week 9 内容
v2.0.0 - 重构整个项目结构
```

---

## 🛡️ 安全推送工作流

### 推荐流程

```bash
# 1. 在重要节点创建标签 (备份点)
git tag -a v1.0.0 -m "Week 1-7 完整内容"
git push origin v1.0.0

# 2. 进行新的开发
git add .
git commit -m "feat: 添加新内容"

# 3. 推送前先拉取远程更新
git pull origin main

# 4. 推送到远程
git push origin main

# 5. 确认无误后创建新版本标签
git tag -a v1.1.0 -m "添加新内容"
git push origin v1.1.0
```

### 使用分支保护

```bash
# 1. 创建开发分支
git checkout -b develop

# 2. 在开发分支上工作
git add .
git commit -m "feat: 新功能"
git push origin develop

# 3. 测试无误后合并到 main
git checkout main
git merge develop
git tag -a v1.1.0 -m "新版本"
git push origin main --tags
```

---

## 🚨 紧急回滚步骤

如果推送了错误的内容,立即执行:

```bash
# 1. 查看版本标签,找到正确的版本
git tag

# 2. 回滚到正确版本
git reset --hard v1.0.0

# 3. 强制推送覆盖错误内容
git push -f origin main

# 4. 验证远程仓库
# 访问 https://github.com/ccboom/Web3_learning 确认
```

---

## 📊 版本历史

| 版本 | 日期 | 提交 SHA | 描述 |
|:---|:---|:---|:---|
| v1.0.0 | 2026-01-23 | f065546 | Week 1-7 完整学习内容 |

---

## 💡 最佳实践

1. **定期打标签**: 在每个重要里程碑创建版本标签
2. **使用语义化版本**: 遵循 Major.Minor.Patch 规范
3. **编写清晰的标签信息**: 描述该版本的主要变更
4. **推送前先标签**: 在推送重要更新前先创建标签备份
5. **使用分支开发**: 在 develop 分支开发,测试后合并到 main
6. **避免频繁强制推送**: 只在必要时使用 `git push -f`

---

## 🔗 相关命令速查

```bash
# 查看标签
git tag                          # 列出所有标签
git tag -l "v1.*"               # 列出 v1.x 版本
git show v1.0.0                 # 查看标签详情

# 创建标签
git tag -a v1.0.0 -m "描述"     # 创建带注释标签
git tag v1.0.0                  # 创建轻量标签

# 推送标签
git push origin v1.0.0          # 推送单个标签
git push origin --tags          # 推送所有标签

# 删除标签
git tag -d v1.0.0               # 删除本地标签
git push origin :refs/tags/v1.0.0  # 删除远程标签

# 回滚
git reset --hard v1.0.0         # 回滚到标签
git reset --hard HEAD~1         # 回滚到上一个提交
git revert <commit-sha>         # 创建回滚提交
```

---

## 📞 获取帮助

- [Git 官方文档 - 标签](https://git-scm.com/book/zh/v2/Git-基础-打标签)
- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [GitHub 发布管理](https://docs.github.com/zh/repositories/releasing-projects-on-github)
